---
 - hosts: all
   become: yes
   tasks:
    - name: install docker
      command:
        cmd: sudo apt install docker.io -y
    - name: install docker-compose
      command:
        cmd: sudo apt install docker-compose -y
